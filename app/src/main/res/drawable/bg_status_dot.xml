<!-- Server-status indicator; tinted at runtime (muted / success / danger). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/colorMuted" />
    <size
        android:width="10dp"
        android:height="10dp" />
</shape>
