<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Lets the status card open the system "allow background" dialog, so the
         keepalive job may restart the service on Android 12+. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
        allowBackup=false: the rule store and the encrypted SorinFlow settings are
        bound to this device; a restored copy without its Keystore key would be
        unreadable anyway. usesCleartextTraffic=false: SorinFlow endpoints are
        https-only, and a plain-http rule fails at connect time rather than
        leaking a one-time code.
    -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <!--
            specialUse (Android 14+): keeps the process alive so codes are forwarded
            within seconds; unlike dataSync it carries no daily runtime limit. The
            property documents the sub-type as Android 14 asks.
        -->
        <service
            android:name=".SmsReceiverService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the SMS forwarder alive so one-time codes reach the server within seconds" />
        </service>

        <!--
            SMS delivery is driven by this manifest-declared receiver, not by a
            receiver registered at runtime inside SmsReceiverService. SMS_RECEIVED
            is on the implicit-broadcast allowlist, so a manifest receiver still
            fires on Android 8+ and, crucially, the system cold-starts the process
            to deliver it even after the foreground service has been killed by an
            aggressive OEM battery manager (issue #78). The android:permission
            guard ensures only the system (holder of BROADCAST_SMS) can trigger it,
            so another app cannot spoof fake SMS into the forwarder.
        -->
        <receiver
            android:name=".SmsBroadcastReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--
            Exported only for the sorinflow://setup deep link (a panel QR code
            scanned with the camera app). The link merely pre-fills the form; the
            user still reviews and saves.
        -->
        <activity
            android:name=".SetupActivity"
            android:exported="true"
            android:label="@string/title_setup"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="setup"
                    android:scheme="sorinflow" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DeliveryLogActivity"
            android:exported="false"
            android:label="@string/title_delivery_log" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/title_settings" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
